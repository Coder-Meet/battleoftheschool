# Branchseed — current fusion submission kit

Extract the whole ZIP and open `START_HERE.html`. This kit supersedes the
earlier strict presentation and Devpost packs. The minimal judge application
is a separate download.

## Give Devpost these items

1. **Name:** Branchseed — Aorta Explorer.
2. **Elevator pitch:** From CT to a map you can inspect: discover aortic branch origins, verify predictions in 3D and linked CT, and export measurements offline.
3. **Track:** Toralis Labs Healthcare. Add every teammate and your school.
4. **Story:** copy section 3 of `DEVPOST_SUBMISSION.md`, `.html` or `.docx`.
   Copy “Built with” and credits from that document.
5. **Repository:** https://github.com/Coder-Meet/battleoftheschool
6. **Video:** upload `branchseed-showcase.mp4` to an accepted public host.
   Test the final link while signed out. Historical interface footage is
   preserved; on-screen counts and timings are not current algorithm results.
7. **Images:** use the PNGs in `docs/media/` with section 5's captions.
8. **Slides:** upload `presentation/branchseed-editable.pptx` or
   `branchseed-slides.pdf` if the form accepts them.
9. **Technical download:** link the selected
   [Windows fusion judge application](https://github.com/Coder-Meet/battleoftheschool/releases/download/branchseed-judge-fusion-2026-09-13/branchseed-judge-fusion-windows-x64.zip)
   and [prediction/verification evidence](https://github.com/Coder-Meet/battleoftheschool/releases/download/branchseed-judge-fusion-2026-09-13/branchseed-judge-fusion-evidence.zip).
   Do not substitute the historical strict application.

## Rehearse and present

- `SPEAKER_SCRIPT.pdf`, `.docx`, `.html` and `.md` contain the timed script.
- Eight slides, exactly five minutes; each of four speakers has 75 seconds.
- Speaker 3 runs the live Explorer at 2:30–3:45. Follow
  `presentation/LIVE_DEMO_CUES.md`; skip the interior tour if behind.
- Use `presentation/index.html` offline with its `assets/` directory beside it.
- PowerPoint is editable; its notes contain the same script. Install the two
  TTF fonts in `presentation/assets/` for matching typography.
- Use the slide PDF if the laptop's slide software differs.
- The showcase is a separate Devpost video; no video plays during the talk.
- `PRESENTER_BRIEFING.pdf` covers the algorithms, caveats and judge questions.
- `DEMO_GUIDE.html` gives current-checkout Explorer setup. The judge ZIP contains
  only inference code/model/wheels; prepare the website separately.

## Scores and remaining actions

Use the fusion evidence in the current deck and scorecard. Reference,
synthetic and runtime results have separate scopes. Earlier strict values in
the briefing are labelled comparisons. Complete the actual Windows-laptop
offline/timing check and final Devpost submission; those remain team actions.

`SHA256SUMS` records the files in this kit. To verify after extraction, run
`sha256sum -c SHA256SUMS` where that command is available.
